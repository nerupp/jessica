# Copy this file to `.env.local` and fill in the values. Do NOT commit real secrets.

# Supabase (public keys for client usage)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Supabase (server-side role key - keep private and never commit)
SUPABASE_SERVICE_ROLE_KEY=

# Model / LLM provider API key (e.g., OpenAI)
MODEL_API_KEY=

# App-specific configuration
NEXT_PUBLIC_APP_NAME=ai-summary-app

# Notes:
# - Keep secrets out of the repo; use GitHub Secrets or Codespaces secrets for CI and deployment.
# - Example: copy to `.env.local` for local development and add it to `.gitignore` so it isn't committed.
